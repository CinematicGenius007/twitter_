{
  "name": "twitter-rebuild",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently -n web,convex -c magenta,green \"bun run --cwd apps/web dev\" \"bun run --cwd apps/web convex:dev\""
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
