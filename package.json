{
  "name": "twitter-rebuild",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently -n api,web -c blue,magenta \"bun run --cwd apps/api dev\" \"bun run --cwd apps/web dev\"",
    "db:init": "bun run --cwd apps/api db:init",
    "db:seed": "bun run --cwd apps/api db:seed"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
