<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title>500 Internal Server Error</title>
    </head>
    <body>
        <div>
            <h1>500 Internal Server Error in <% if (url) { %><%= url %><% } %></h1>
            <p>There was an internal server error.</p>
            <% if (error) { %>                
                <p><%= error %></p>
            <% } %>
        </div>
    </body>
</html>